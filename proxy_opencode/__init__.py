"""proxy_opencode: a compliant OpenAI-compatible reverse proxy gateway.

This gateway only forwards requests to a configured OpenAI-compatible
upstream. It does NOT bypass rate limits, abuse free tiers, or spoof
client fingerprints.
"""

__version__ = "0.1.0"
